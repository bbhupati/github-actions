name: build and deploy

on:
  push:
  workflow_dispatch:

# jobs:
#   pre-job:
#     uses: bbhupati/poc-action-templates/.github/workflows/set-env.yml@master
#   npm:
#     needs: pre-job
#     uses: bbhupati/poc-action-templates/.github/workflows/npm-build.yml@master
#   docker:
#     needs: [pre-job,npm]
#     uses: bbhupati/poc-action-templates/.github/workflows/docker-build-push.yml@master


jobs:
  example_job:
    name: Generate output
    runs-on: ubuntu-latest
    steps:
      - id: step1
        run: |
          echo "firstword=hello" >> $GITHUB_ENV
          source $GITHUB_ENV  # Load the environment variables
          echo "env: $firstword"  # This should now work



  job2:
    needs: example_job
    uses: bbhupati/poc-action-templates/.github/workflows/values.yml@master

